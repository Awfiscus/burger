<h1>Have A Burger!!</h1>

<img src="https://images.unsplash.com/photo-1568901346375-23c9450c58cd?ixid=MXwxMjA3fDB8MHxzZWFyY2h8MXx8YnVyZ2VyfGVufDB8fDB8&ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=60"
    alt="burger" id="burgerImage">

<div class="class" id="readytoEat">
    <h2>Burgers to EAT!!!!</h2>
    <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burger-block devoured=false}}
        {{/unless}}
        {{/each}}
    </ul>
</div>


<div class="container" id="listofeatenburgers">
    <h2>Burgers that have been eaten!</h2>
    <ul>
        {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burger-block devoured=true}}
        {{/if}}
        {{/each}}
    </ul>
</div>

<div class="class" id="createBurger">
    <h2>Add a Burger to Eat!</h2>

    <form id='create-form'>
        <div>
            <label for="burg">Burger Type:</label>
            <input type="text" id="burg" name="burger_name">
        </div>
        <button type="submit">Add Burger to Be Eaten!!</button>
    </form>
</div>